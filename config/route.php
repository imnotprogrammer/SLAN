<?php
/**
 * 路由配置信息
 * */
 
    return array(
        'defaultController' => 'lan',       //缺省访问的控制器名称
        'defaultAction' => 'index',         //缺省的访问方法名称
        'defaultroute'   =>  'r'            /*获取路由的缺省方式，此处可以设置为r和/两种值；
                                            'r'代表利用r=controller/action,'/'代表 /controller/action*/
    );

?>
